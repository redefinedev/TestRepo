{
  "name": "Mocha test repo",
  "version": "1.0.0",
  "main": "src/index.js",
  "directories": {
    "test": "./test"
  },
  "scripts": {
    "test": "mocha test/** --recursive",
    "test-subdir": "mocha test/foo/** --recursive",
    "test-subdir-2": "mocha test/foo/bar/** --recursive"
  },
  "engines": {
    "node": ">=10.0.0"
  },
  "devDependencies": {
    "mocha": "latest"
  }
}
